schema_version: bpfix.case/v1
case_id: stackoverflow-79485758
source:
  kind: stackoverflow
  url: https://stackoverflow.com/questions/79485758
  repository: null
  commit: null
  upstream_file: null
  collected_at: null
  raw_excerpt_files:
  - bpfix-bench/raw/so/stackoverflow-79485758.yaml
reproducer:
  reconstruction: reconstructed
  source_file: prog.c
  build_command: make
  object_path: prog.o
  load_command: make replay-verify
  notes: Imported from reconstructed external source into bpfix-bench; replay logs
    are generated as replay-verifier.log.
capture:
  capture_id: stackoverflow-79485758__kernel-6.15.11-clang-18-log2
  environment_id: kernel-6.15.11-clang-18-log2
  verifier_log: replay-verifier.log
  capture_metadata: capture.yaml
  log_quality: trace_rich
  terminal_error: invalid access to packet, off=0 size=2, R5(id=6242,off=0,r=0)
  rejected_insn_idx: 34
external_match:
  status: exact
  policy: terminal_error
  matched_messages:
  - invalid access to packet, off=0 size=2, R5(id=6,off=0,r=0)
  - invalid access to packet, off=0 size=2, R5(id=6242,off=0,r=0)
  notes: Imported only because verifier_error_match was exact/partial in the verified
    SO/GH artifact.
label:
  capture_id: stackoverflow-79485758__kernel-6.15.11-clang-18-log2
  taxonomy_class: verifier_false_positive
  mechanism_tags:
  - packet_bounds
  - verifier_range_precision
  evidence_tags:
  - source_check_present
  - verifier_max_packet_offset_corner_case
  error_id: BPFIX-E005
  confidence: medium
  label_source: adjudicated
  root_cause_description: The packet parser checks `data + field_offset + sizeof(uint16_t)
    <= data_end`, but the verifier still treats `field_offset` as too wide for
    its packet-offset precision model and rejects the later 16-bit load.
  root_cause_insn_idx: 44
  rejected_line: x = *((uint16_t*) (data + field_offset));
  root_cause_line: if ((data + field_offset + sizeof(uint16_t)) > data_end)
  localization_confidence: medium
  fix_type: type_cast
  fix_direction: Clamp `field_offset` to a non-negative upper-bounded value before
    pointer arithmetic, then rederive the checked packet pointer from that bounded
    offset immediately before the 16-bit load.
repair:
  eligible: true
reporting:
  family_id: stackoverflow-79485758
  representative: true
  notes: External SO/GH case imported from verified exact/partial artifact.
