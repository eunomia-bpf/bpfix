schema_version: bpfix.case/v1
case_id: stackoverflow-68752893
source:
  kind: stackoverflow
  url: https://stackoverflow.com/questions/68752893
  repository: null
  commit: null
  upstream_file: null
  raw_excerpt_files:
  - bpfix-bench/raw/so/stackoverflow-68752893.yaml
reproducer:
  reconstruction: reconstructed
  notes: Imported from reconstructed external source into bpfix-bench; replay logs are generated
    as replay-verifier.log.
capture:
  capture_id: stackoverflow-68752893__kernel-6.15.11-clang-18-log2
  terminal_error: invalid access to packet, off=23 size=1, R1(id=0,off=23,r=0)
  rejected_insn_idx: 3
external_match:
  status: exact
  policy: terminal_error
  matched_messages:
  - invalid access to packet, off=23 size=1, R1(id=0,off=23,r=0)
  - invalid access to packet, off=23 size=1, R1(id=0,off=23,r=0)
  notes: Promoted after a fresh raw SO/GH rebuild matched the locally captured terminal verifier
    error exactly; the earlier mismatch came from a coarse loader/permission line rather than
    the terminal verifier error.
label:
  capture_id: stackoverflow-68752893__kernel-6.15.11-clang-18-log2
  taxonomy_class: source_bug
  error_id: BPFIX-E001
  confidence: high
  label_source: agree
  root_cause_description: The XDP program computes `struct iphdr *ip = (struct iphdr *)((char
    *)ctx->data + ipsize)` and reads `ip->protocol` without first proving that `data + ipsize
    + sizeof(*ip)` stays within `data_end`.
  root_cause_insn_idx: null
  localization_confidence: high
  fix_type: bounds_check
  fix_direction: Add a packet-length guard for the full IP-header access before reading `ip->protocol`
    or any other IP fields.
repair:
  eligible: true
reporting:
  family_id: stackoverflow-68752893
  representative: true
  notes: External SO/GH case promoted after fresh exact terminal-error replay audit.
