schema_version: bpfix.case/v1
case_id: stackoverflow-74178703
source:
  kind: stackoverflow
  url: https://stackoverflow.com/questions/74178703
  repository: null
  commit: null
  upstream_file: null
  raw_excerpt_files:
  - bpfix-bench/raw/so/stackoverflow-74178703.yaml
reproducer:
  reconstruction: reconstructed
  notes: 'Imported from reconstructed external source into bpfix-bench; replay logs are generated
    as replay-verifier.log. Compatibility rewrite: converted legacy bpf_elf_map to BTF SEC(".maps")
    definitions; made offset/size verifier-visible scalars and preserved the copied byte so
    clang cannot optimize away the map-value read.'
capture:
  capture_id: stackoverflow-74178703__kernel-6.15.11-clang-18-log2
  terminal_error: invalid access to map value, value_size=1024 off=1024 size=1
  rejected_insn_idx: 34
external_match:
  status: semantic
  policy: terminal_error
  matched_messages:
  - invalid access to map value, value_size=1024 off=1024 size=1
  - 'Error: failed to open object file'
  notes: Original external report text did not exactly match this kernel's terminal verifier
    error; the case is included because the local replay produces a trace-rich verifier rejection.
label:
  capture_id: stackoverflow-74178703__kernel-6.15.11-clang-18-log2
  taxonomy_class: lowering_artifact
  error_id: BPFIX-E005
  confidence: medium
  label_source: agree
  root_cause_description: The code checks `offset + i >= 1024` but then rebuilds the access
    as `b + offset + i` for `memcpy(dst + i, b + i, 1)`, and the verifier cannot keep that
    cross-variable relation, so the map-value access still appears able to reach offset 1024.
  root_cause_insn_idx: null
  rejected_line: memcpy(dst + i, b + offset + i, sizeof(__u8));
  root_cause_line: for (__u32 i = 0; i < size && i < 512; ++i) {
  localization_confidence: high
  fix_type: reorder
  fix_direction: Use one bounded index variable for the final load/store address, or split
    the copy so the same checked expression is reused at the access site.
repair:
  eligible: true
reporting:
  family_id: stackoverflow-74178703
  representative: true
  notes: External SO/GH case imported as a replay-valid semantic match.
