schema_version: bpfix.case/v1
case_id: stackoverflow-67679109
source:
  kind: stackoverflow
  url: https://stackoverflow.com/questions/67679109
  repository: null
  commit: null
  upstream_file: null
  raw_excerpt_files:
  - bpfix-bench/raw/so/stackoverflow-67679109.yaml
reproducer:
  reconstruction: reconstructed
  notes: Imported from reconstructed external source into bpfix-bench; replay logs are generated
    as replay-verifier.log.
capture:
  capture_id: stackoverflow-67679109__kernel-6.15.11-clang-18-log2
  terminal_error: R8 invalid mem access 'scalar'
  rejected_insn_idx: 58
external_match:
  status: exact
  policy: terminal_error
  matched_messages:
  - R8 invalid mem access 'scalar'
  - R8 invalid mem access 'scalar'
  notes: Promoted after a fresh raw SO/GH rebuild matched the locally captured terminal verifier
    error exactly; the earlier mismatch came from a coarse loader/permission line rather than
    the terminal verifier error.
label:
  capture_id: stackoverflow-67679109__kernel-6.15.11-clang-18-log2
  taxonomy_class: source_bug
  error_id: BPFIX-E011
  confidence: medium
  label_source: agree
  root_cause_description: After copying user memory into `str`, the CRC loop dereferences
    `*current++` even though `current` is not a verifier-proven pointer to that stack buffer,
    so the load is treated as an invalid scalar dereference.
  root_cause_insn_idx: 58
  rejected_line: crc ^= *current++;
  root_cause_line: crc ^= *current++;
  localization_confidence: high
  fix_type: other
  fix_direction: Make the loop walk the copied stack buffer explicitly, for example by setting
    `current = str` and keeping all byte reads on that verified stack object.
repair:
  eligible: true
reporting:
  family_id: stackoverflow-67679109
  representative: true
  notes: External SO/GH case promoted after fresh exact terminal-error replay audit.
