schema_version: bpfix.case/v1
case_id: stackoverflow-71522674
source:
  kind: stackoverflow
  url: https://stackoverflow.com/questions/71522674
  repository: null
  commit: null
  upstream_file: null
  raw_excerpt_files:
  - bpfix-bench/raw/so/stackoverflow-71522674.yaml
reproducer:
  reconstruction: reconstructed
  notes: Imported from reconstructed external source into bpfix-bench; replay logs are generated
    as replay-verifier.log.
capture:
  capture_id: stackoverflow-71522674__kernel-6.15.11-clang-18-log2
  terminal_error: invalid access to packet, off=14 size=60, R3(id=0,off=14,r=34)
  rejected_insn_idx: 19
external_match:
  status: exact
  policy: terminal_error
  matched_messages:
  - invalid access to packet, off=14 size=60, R3(id=1,off=14,r=34)
  - invalid access to packet, off=14 size=60, R3(id=0,off=14,r=34)
  notes: Imported only because verifier_error_match was exact/partial in the verified SO/GH
    artifact.
label:
  capture_id: stackoverflow-71522674__kernel-6.15.11-clang-18-log2
  taxonomy_class: source_bug
  error_id: BPFIX-E005
  confidence: medium
  label_source: agree
  root_cause_description: The code checks `(void *)tcph + tcp_len <= data_end`, but the variable
    `tcp_len` proof does not survive into the `bpf_csum_diff(0, 0, (void *)tcph, tcp_len,
    0)` memory/len argument pair, so the helper call is rejected as a packet access beyond
    the proven range.
  root_cause_insn_idx: null
  localization_confidence: low
  fix_type: clamp
  fix_direction: Re-clamp `tcp_len` into a simple bounded unsigned value and place the exact
    `tcph + tcp_len <= data_end` check immediately before `bpf_csum_diff`.
repair:
  eligible: true
reporting:
  family_id: stackoverflow-71522674
  representative: true
  notes: External SO/GH case imported from verified exact/partial artifact.
