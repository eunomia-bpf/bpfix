schema_version: bpfix.case/v1
case_id: github-commit-cilium-3740e9db8fef
source:
  kind: github_commit
  url: https://github.com/cilium/cilium/commit/3740e9db8fef9adc382f9dfecb5c404bda278afb
  repository: https://github.com/cilium/cilium
  commit: 3740e9db8fef9adc382f9dfecb5c404bda278afb
  upstream_file: bpf/lib/lb.h
  raw_excerpt_files:
  - bpfix-bench/raw/gh/github-commit-cilium-3740e9db8fef.yaml
reproducer:
  reconstruction: reconstructed
  notes: Minimal local reproduction of the commit's R2 !read_ok class. The program intentionally
    leaves a map key pointer uninitialized before the helper call, matching the verifier symptom
    that the Cilium fix avoided by always initializing the out-parameter on error paths.
capture:
  capture_id: github-commit-cilium-3740e9db8fef__kernel-6.15.11-clang-18-log2
  terminal_error: R2 !read_ok
  rejected_insn_idx: 2
external_match:
  status: not_applicable
  policy: verifier_symptom
  matched_messages:
  - R2 !read_ok
  notes: Raw record has no full upstream verifier log, but the commit title names this exact
    verifier error and the standalone replay reproduces it.
label:
  capture_id: github-commit-cilium-3740e9db8fef__kernel-6.15.11-clang-18-log2
  taxonomy_class: source_bug
  error_id: BPFIX-E003
  confidence: medium
  label_source: external_replay_reconstruction
  root_cause_description: A helper argument register is consumed before all paths initialize
    the value feeding it.
  root_cause_insn_idx: null
  localization_confidence: medium
  fix_type: initialization
  fix_direction: Initialize the value on every return path before any helper call or spill
    can use it.
reporting:
  family_id: github-commit-cilium-3740e9db8fef
  representative: true
  notes: Current replay artifact passes an uninitialized key pointer to bpf_map_lookup_elem,
    so the executable case is labelled as a source bug even though the external commit record
    may describe a lowering-sensitive workaround.
