# Empirical Corpus Layout Design

Status: implemented  
Date: 2026-04-29

## Position

The empirical corpus should be simple and strict:

> A case is valuable for the primary eval only if it can be reproduced locally
> and its verifier failure is freshly captured by our harness under a pinned
> environment.

Kernel selftests, Stack Overflow, GitHub issues, and commits can all be used as
case discovery sources. They do not define the empirical corpus input. The empirical corpus
input is the locally replayable program plus the expected verifier failure
metadata.

If a case cannot be built, loaded, rejected, and captured locally, it is not part
of the primary empirical corpus. It can be kept as notes or future work, but it should
not affect headline numbers.

## Design Goals

1. Keep one empirical corpus directory, not separate source/reproducer/capture trees.
2. Put everything needed to replay and audit a case under that case's directory.
3. Use one manifest as the only eval entry point.
4. Require every primary case to produce a fresh verifier log during replay.
5. Bind labels to a replay capture through `capture_id`, terminal error, and
   rejected instruction.
6. Require `validate_empirical.py --replay` to rebuild/reload every primary case.
7. Avoid static checksum files; replay is the admission proof.

Canonical `source.kind` values in the new empirical corpus are:

- `kernel_selftest`
- `stackoverflow`
- `github_issue`
- `github_commit`

Legacy names such as `kernel_selftests`, `github_issues`,
`commit_derived`, and `eval_commits` are import-time aliases only. They should
not appear in `bpfix-empirical/`.

## Minimal Directory Layout

```
bpfix-empirical/
├── README.md
├── manifest.yaml
├── environment.yaml
└── cases/
    └── <case_id>/
        ├── case.yaml
        ├── original.log          # optional raw excerpt from source
        ├── original.md           # optional post/issue/selftest notes
        ├── prog.c                # or prog.bpf.c / main.rs / etc.
        ├── Makefile
        ├── headers/              # optional headers/config
        ├── selftest_prog_loader.c # optional source for selftest harness
        ├── capture.yaml          # replay/capture provenance
        ├── build.stdout          # optional retained capture artifact
        ├── build.stderr          # optional retained capture artifact
        ├── load.stdout           # optional retained capture artifact
        ├── load.stderr           # optional retained capture artifact
        ├── fixed/                # optional, only for repair eval
        │   ├── prog.c
        │   ├── Makefile
        │   ├── extra/
        │   ├── build.stdout
        │   ├── build.stderr
        │   ├── load.stdout
        │   └── load.stderr
```

There are no separate `sources/`, `reproducers/`, or `captures/` folders. Those
concepts are fields inside `case.yaml`.

The case root has one buggy program by default. `fixed/` is not part of the
diagnostic schema; it exists only when the case is also admitted to a repair
eval.

## Case Rule

Each case directory is self-contained. A reviewer should be able to inspect one
directory and answer:

- where the case came from;
- what code was run;
- what command built it;
- what command loaded it;
- what verifier log was captured;
- how the label was assigned;
- whether the local failure matches the original external report.

## `manifest.yaml`

Path: `bpfix-empirical/manifest.yaml`

The manifest is the only file eval scripts should read to discover cases.

```yaml
schema_version: bpfix.empirical/v1
empirical_id: bpfix-empirical-v1
frozen_at: "2026-04-29"
environment_id: kernel-6.15.11-clang-18-log2
description: "Locally reproducible verifier-log empirical corpus."

cases:
  - case_id: kernel-selftest-dynptr-invalid-read-2cc2b993
    path: cases/kernel-selftest-dynptr-invalid-read-2cc2b993
    source_kind: kernel_selftest
    family_id: dynptr-invalid-read
    representative: true
    capture_id: kernel-selftest-dynptr-invalid-read-2cc2b993__kernel-6.15.11-clang-18-log2

  - case_id: stackoverflow-70750259
    path: cases/stackoverflow-70750259
    source_kind: stackoverflow
    family_id: packet-pointer-provenance
    representative: true
    capture_id: stackoverflow-70750259__kernel-6.15.11-clang-18-log2

```

There is no `checksums.txt`. Derived summaries such as source counts and
taxonomy counts should be generated by reports, not stored in the manifest.

## `environment.yaml`

Path: `bpfix-empirical/environment.yaml`

```yaml
schema_version: bpfix.environment/v1
environment_id: kernel-6.15.11-clang-18-log2
kernel:
  version: "6.15.11"
  btf_path: /sys/kernel/btf/vmlinux
toolchain:
  clang: "18.1.8"
  llc: "18.1.8"
  bpftool: "v7.5.0"
  libbpf: "1.5.0"
harness:
  name: bpfix-replay
  script: bpfix-empirical/tools/validate_empirical.py
  verifier_log_level: 2
  timeout_sec: 30
runtime:
  arch: x86_64
  requires_root_or_cap_bpf: true
```

One frozen empirical corpus version uses one primary environment. Cross-kernel replay
can be a separate empirical corpus version or a separate appendix, not the default
layout.

## `case.yaml`

Path: `bpfix-empirical/cases/<case_id>/case.yaml`

The validator expands `manifest.yaml.case_defaults` before replaying or scoring
a case. Fields that are constant across the frozen corpus, such as `prog.c`,
`prog.o`, `make`, `make replay-verify`, `replay-verifier.log`, `capture.yaml`,
and `trace_rich`, should stay in the manifest defaults instead of being repeated
in every case.

```yaml
schema_version: bpfix.case/v1
case_id: stackoverflow-70750259

source:
  kind: stackoverflow       # kernel_selftest | stackoverflow | github_issue | github_commit
  url: https://stackoverflow.com/questions/70750259
  repository: null
  commit: null
  upstream_file: null
  upstream_section: null
  collected_at: "2026-03-13"
  raw_excerpt_files:
    - original.md
    - original.log

reproducer:
  reconstruction: original  # original | minimized | reconstructed
  program_type: socket_filter
  notes: "Build glue added around the original code snippet."

capture:
  capture_id: stackoverflow-70750259__kernel-6.15.11-clang-18-log2
  terminal_error: "math between pkt pointer and register with unbounded min value is not allowed"
  rejected_insn_idx: 39

external_match:
  status: partial           # exact | partial | semantic | not_applicable
  policy: terminal_error
  matched_messages:
    - "math between pkt pointer and register with unbounded min value is not allowed"
  notes: "Terminal verifier error matches the original post excerpt."

label:
  capture_id: stackoverflow-70750259__kernel-6.15.11-clang-18-log2
  taxonomy_class: lowering_artifact
  error_id: BPFIX-E005
  confidence: high
  label_source: adjudicated
  root_cause_description: "Compiler/verifier-unfriendly lowering loses packet-bound proof before pointer arithmetic."
  root_cause_insn_idx: 28
  rejected_line: "..."
  root_cause_line: "..."
  localization_confidence: high
  fix_type: reorder
  fix_direction: "Move the bounds check so the verifier can connect the checked length to the packet access."

reporting:
  family_id: packet-pointer-provenance
  duplicate_group: null
  representative: true
  tags:
    - external_user_report
    - reconstructed
```

Omit `repair` when no repair oracle exists. A `repair` block is present only for
repair-eligible cases.

The label repeats `capture_id` on purpose. Localization labels depend on the
captured verifier failure, so the validator must reject a case if replay drifts
from the recorded terminal error or rejected instruction.

## Primary Case Admission Rules

A case enters `bpfix-empirical` only if all conditions hold:

1. The case is listed in `bpfix-empirical/manifest.yaml`; blocked and retired
   reproducers stay outside the frozen empirical corpus manifest.
2. `reproducer.reconstruction` is `original`, `minimized`, or `reconstructed`.
3. The buggy program builds in the pinned environment.
4. The buggy program is rejected by the verifier in the pinned environment.
5. A verifier log is captured locally by the replay harness.
6. Harness verifier log level is `2` or an explicitly documented equivalent.
7. The parser can recover a terminal error from the fresh replay log.
8. The parser can recover or validate `capture.rejected_insn_idx`.
9. Any non-null `label.root_cause_insn_idx` appears in the stored local replay
   verifier log. Root-cause indices use the same instruction numbering as
   `capture.rejected_insn_idx`; original external-log instruction numbers must
   stay in raw audit records such as `raw/legacy-insn-numbering.yaml`.
10. `label.capture_id == capture.capture_id`.
11. For Stack Overflow and GitHub cases, `external_match.status` is `exact`,
    `partial`, or `semantic`. `semantic` is allowed only when fresh local replay
    produces a trace-rich verifier rejection; it is not a candidate split.
12. For kernel selftests, `external_match.status == not_applicable`.
13. Commit-derived cases require manual audit and
    `external_match.status == not_applicable`.
14. `reporting.family_id` and `reporting.representative` are present.
15. `bpfix-empirical/tools/validate_empirical.py --replay bpfix-empirical` succeeds for the case
    after applying `manifest.yaml.case_defaults`.

No exception path should allow excerpt-only logs, message-only loader errors, or
synthetic no-log cases into `bpfix-empirical`.

## Repair Case Rules

Repair eval is separate from the empirical corpus. A case can be in the
empirical corpus without any fixed program.

A case is eligible for repair eval only if:

1. `repair.eligible == true`.
2. `repair.fixed_dir` exists.
3. The fixed program builds in the same pinned environment.
4. The fixed program loads successfully, or the case declares a stricter repair
   oracle in `repair.fixed_oracle`.
5. Fixed build/load stdout and stderr are stored under `fixed/`.

Diagnostic eval must ignore the `repair` block. Repair eval must ignore cases
without `repair.eligible == true`; an omitted `repair` block means the diagnostic
case has no repair oracle.

## Non-Primary Cases

Cases that cannot be reproduced locally should not be stored in the primary
empirical corpus directory. Keep them in a separate scratch or legacy area until they
become reproducible.

Recommended handling:

- Stack Overflow/GitHub excerpt but no buildable reproducer: not in
  `bpfix-empirical`.
- Commit diff but no verifier reproduction: not in `bpfix-empirical`.
- Synthetic case without verifier log: not in `bpfix-empirical`.
- Case accepted by the current verifier: not in `bpfix-empirical`; optionally keep
  outside the empirical corpus as cross-version evidence.
- Case with only message-level stderr and no trace-rich verifier log: not in
  `bpfix-empirical`.

This keeps the empirical corpus honest: the main number measures diagnosis over local,
auditable verifier traces.

## Validator

Add one replay validator before moving eval scripts:

```
python3 bpfix-empirical/tools/validate_empirical.py --replay bpfix-empirical
```

`bpfix-empirical/tools/validate_empirical.py` must rerun build/load/capture for every listed
case. Static schema checks are useful, but they are not sufficient for
admission into the empirical corpus.

For each primary case, the validator must:

1. run the case's `reproducer.build_command`;
2. run the case's `reproducer.load_command` under the pinned environment;
3. capture a fresh temporary verifier log with the configured log level;
4. confirm the load result is `verifier_reject`;
5. parse the fresh log and recover terminal error, log quality, and rejected
   instruction index;
6. compare the fresh terminal error with `capture.terminal_error`;
7. compare the fresh rejected instruction with `capture.rejected_insn_idx`, or
   fail if the case declares no stable rejected instruction;
8. compare fresh log quality with `capture.log_quality`;
9. write a replay report that records pass/fail status for every case.

The validator should fail on:

- missing files referenced by `manifest.yaml`;
- duplicate `case_id`;
- missing `capture.yaml`;
- `capture.yaml` command/environment mismatch with `case.yaml` or
  `environment.yaml`;
- non-`verifier_reject` case;
- missing terminal error;
- missing rejected instruction index;
- unsupported `source.kind`;
- external case without acceptable `external_match.status`;
- case with `reconstruction: synthetic`;
- missing `family_id` or `representative`;
- build failure during replay;
- load acceptance during replay;
- infrastructure error during replay;
- fresh replay terminal error mismatch;
- fresh replay rejected-instruction mismatch;
- fresh replay log quality below the recorded requirement.

Eval scripts should refuse to run unless the empirical corpus has a passing replay
validator report for the same `environment_id`, `manifest.yaml`, and case set.

## Eval Script Contract

Eval scripts should take an empirical corpus path. This is currently implemented
for the diagnostic entry point:

```
python3 docs/evaluation/evaluate_diagnostics.py --empirical-root bpfix-empirical
```

They should:

1. run `bpfix-empirical/tools/validate_empirical.py --replay bpfix-empirical`, or require a passing
   replay report generated from the same manifest and environment;
2. read `manifest.yaml`;
3. load each case's fresh `replay-verifier.log` generated by the validator;
4. join labels from the same case's `case.yaml`;
5. write result rows with `empirical_id`, `case_id`, `capture_id`,
   `source_kind`, `family_id`, and `representative`.

No eval script should recursively scan `case_study/cases/*` for headline
numbers.

## Import Policy From Current Corpus

Migration cost is not a design constraint. The import rule is:

> Import only cases that can satisfy the new primary admission rules.

Current data should be treated as raw material:

- `case_study/cases/kernel_selftests_verified/<case_id>/`: likely easiest to
  import if it has source, build command, rejection status, and captured log.
- `case_study/cases/so_gh_verified/<case_id>/`: import exact/partial matches
  directly; import mismatch cases as `external_match.status: semantic` only
  when a fresh local replay produces a trace-rich verifier rejection. Loader
  errors, libbpf object-open errors, timeouts without a verifier terminal error,
  and current-kernel accepts are not empirical corpus cases.
- `case_study/cases/eval_commits_verified/<case_id>/`: import only a concrete
  commit-derived variant that builds, is rejected locally, and has a captured
  trace-rich verifier log. A fixed/buggy pair where both pass is not a
  empirical corpus case.
- `case_study/cases/stackoverflow/*.yaml` and `github_issues/*.yaml`: use only
  as discovery and original evidence; do not import excerpt-only cases.
- `case_study/cases/eval_commits_synthetic/*.yaml`: do not import unless a real
  local verifier rejection is produced.

Old manifests and old YAML schemas do not need to be preserved inside
`bpfix-empirical`. If old results are needed, keep them under legacy docs; they
should not shape the new empirical corpus.

## Build Order

1. Create `bpfix-empirical/environment.yaml`.
2. Pick a small seed set of reproducible cases, preferably 20 selftests and 10
   external SO/GH cases.
3. Convert each seed into one self-contained case directory.
4. Generate `case.yaml` and replay/capture metadata for each case.
5. Write `manifest.yaml`.
6. Implement `validate_empirical.py`.
7. Run the validator and fix every failure.
8. Update eval scripts to consume `bpfix-empirical`.
9. Freeze the first real empirical corpus only after validator passes.
10. Expand case count after the structure is proven.

This deliberately favors a small clean empirical corpus over a large mixed corpus.

## Current Snapshot

The implemented `bpfix-empirical/` snapshot has:

- 235 replayable cases for headline diagnostic eval.
- 85 `kernel_selftest` cases.
- 86 `stackoverflow` cases.
- 64 GitHub-derived cases: 46 from commits and 18 from issues.
- No case directories outside `manifest.yaml`.

The replay validator has been run successfully:

```bash
python3 bpfix-empirical/tools/validate_empirical.py --replay bpfix-empirical --timeout-sec 60
# passed: 235, failed: 0
```

The diagnostic eval adapter has also been run successfully:

```bash
python3 bpfix-empirical/run-bpfix-eval.py --coverage
# processed: 235 cases
```

## Reporting Requirements

Every public evaluation result using this empirical corpus should report:

- empirical ID;
- environment ID;
- number of primary cases;
- source composition;
- taxonomy distribution;
- reconstruction distribution: original, minimized, reconstructed;
- external match distribution: exact, partial, semantic, not applicable;
- duplicate-family counts;
- case-weighted and family-weighted metrics.

The main claim should use only `bpfix-empirical/manifest.yaml`. There is no
headline metric over unreproduced external excerpts.

## Why This Is Simpler

The design has only one new empirical corpus directory and one case format. It avoids
maintaining separate source, reproducer, capture, and label trees. The tradeoff
is intentional: any case that cannot be normalized into this format is excluded
from the primary eval.

That tradeoff is appropriate for a systems-tool evaluation. A smaller empirical corpus
with uniform local reproduction is more defensible than a larger empirical corpus with
mixed log provenance.
