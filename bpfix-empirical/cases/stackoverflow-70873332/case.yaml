schema_version: bpfix.case/v1
case_id: stackoverflow-70873332
source:
  kind: stackoverflow
  url: https://stackoverflow.com/questions/70873332
  repository: null
  commit: null
  upstream_file: null
  raw_excerpt_files:
  - bpfix-empirical/raw/so/stackoverflow-70873332.yaml
reproducer:
  reconstruction: reconstructed
  notes: Imported from reconstructed external source into bpfix-empirical; replay logs are generated
    as replay-verifier.log.
capture:
  capture_id: stackoverflow-70873332__kernel-6.15.11-clang-18-log2
  terminal_error: invalid access to packet, off=0 size=1, R1(id=1,off=0,r=0)
  rejected_insn_idx: 18
external_match:
  status: exact
  policy: terminal_error
  matched_messages:
  - invalid access to packet, off=0 size=1, R1(id=2,off=0,r=0)
  - invalid access to packet, off=0 size=1, R1(id=1,off=0,r=0)
  notes: Imported only because verifier_error_match was exact/partial in the verified SO/GH
    artifact.
label:
  capture_id: stackoverflow-70873332__kernel-6.15.11-clang-18-log2
  taxonomy_class: verifier_false_positive
  mechanism_tags:
  - packet_bounds
  - verifier_range_precision
  evidence_tags:
  - source_check_present
  - verifier_max_packet_offset_corner_case
  error_id: BPFIX-E005
  confidence: medium
  label_source: agree
  root_cause_description: The loop checks `data + pkt_ctx->pkt_offset + j + 1 <= data_end`,
    but the verifier refuses to apply that packet-length proof because the map-derived offset
    range can cross the `MAX_PACKET_OFF` precision boundary.
  root_cause_insn_idx: 17
  rejected_line: if (data[pkt_ctx->pkt_offset + j] == '\r') {
  root_cause_line: if (data_end < data + pkt_ctx->pkt_offset + j + 1) {
  localization_confidence: medium
  fix_type: clamp
  fix_direction: Bound `pkt_ctx->pkt_offset` separately before the loop and keep the checked
    packet pointer in the same register that is later dereferenced.
repair:
  eligible: true
reporting:
  family_id: stackoverflow-70873332
  representative: true
  notes: External SO/GH case imported from verified exact/partial artifact.
