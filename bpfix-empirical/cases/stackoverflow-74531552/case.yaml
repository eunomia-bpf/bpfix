schema_version: bpfix.case/v1
case_id: stackoverflow-74531552
source:
  kind: stackoverflow
  url: https://stackoverflow.com/questions/74531552
  repository: null
  commit: null
  upstream_file: null
  raw_excerpt_files:
  - bpfix-empirical/raw/so/stackoverflow-74531552.yaml
reproducer:
  reconstruction: reconstructed
  notes: Imported from reconstructed external source into bpfix-empirical; replay logs are generated
    as replay-verifier.log.
capture:
  capture_id: stackoverflow-74531552__kernel-6.15.11-clang-18-log2
  terminal_error: math between fp pointer and register with unbounded min value is not allowed
  rejected_insn_idx: 68
external_match:
  status: exact
  policy: terminal_error
  matched_messages:
  - math between fp pointer and register with unbounded min value is not allowed
  - math between fp pointer and register with unbounded min value is not allowed
  notes: Promoted after a fresh raw SO/GH rebuild matched the locally captured terminal verifier
    error exactly; the earlier mismatch came from a coarse loader/permission line rather than
    the terminal verifier error.
label:
  capture_id: stackoverflow-74531552__kernel-6.15.11-clang-18-log2
  taxonomy_class: source_bug
  error_id: BPFIX-E023
  confidence: low
  label_source: adjudicated
  root_cause_description: The state-machine code checks `next`, but it still uses the current
    state value to form a shifted table offset without first clamping that current state into
    the valid automaton range, so the verifier sees unsafe register-derived offset arithmetic.
  root_cause_insn_idx: null
  localization_confidence: high
  fix_type: clamp
  fix_direction: Add a dominating range check on the current state value before using it to
    index the automaton tables or build the shifted offset.
repair:
  eligible: true
reporting:
  family_id: stackoverflow-74531552
  representative: true
  notes: External SO/GH case promoted after fresh exact terminal-error replay audit.
